{
  "name": "@zeitgeistpm/tx-export",
  "version": "0.0.0",
  "description": "An npm package using subscan.io to export zeitgeist.pm transaction data in a token tracking tool format.",
  "main": "dist/index.js",
  "repository": "https://github.com/Chralt98/zeitgeistpm-tx-export.git",
  "author": "Chralt98 <chralt98@gmail.com>",
  "license": "GPL-3.0",
  "private": false,
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^20.11.13",
    "@typescript-eslint/eslint-plugin": "^6.4.0",
    "@typescript-eslint/parser": "^6.20.0",
    "eslint": "^8.0.1",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ignore-path .eslintignore --ext .js,.ts",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(js|ts|json)\"",
    "prepublishOnly": "yarn build"
  }
}
